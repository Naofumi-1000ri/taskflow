interface:
  display_name: "Issue to Code"
  short_description: "Turn a scoped issue into a tested PR handoff"
  default_prompt: "Use $taskflow-github-delivery issue-to-code to triage a scoped issue, implement it, run checks, and open a PR for review."
