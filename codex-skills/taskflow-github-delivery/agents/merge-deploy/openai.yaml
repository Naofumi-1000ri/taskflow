interface:
  display_name: "Merge and Deploy"
  short_description: "Merge an approved PR and verify main plus deploy"
  default_prompt: "Use $taskflow-github-delivery merge-deploy to merge an accepted PR, watch main, and confirm the deploy result."
